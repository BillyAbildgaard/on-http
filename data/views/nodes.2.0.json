{
[<% for (var i = 0; i < body.length; i++) {%>
    "Name":<%= body[i].name%>,
    "Identifiers": [ <%= body[i].name %> ],
    "type": <%= body[i].type %>,
    "autoDiscover": <%= body[i].autoDiscover %>,
    "CreatedAt": <%= body[i].createdAt %>,
    "updatedAt": <%= body[i].updatedAt %>,
    "Id": <%= body[i].id %> <% } %>
]
}
